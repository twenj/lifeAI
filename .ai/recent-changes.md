# Recent Changes

## 2026-07-12（傍晚）
- Project Memory MCP：项目级 `.cursor/mcp.json` 已连通（`project-0-lifeAI-project-memory`）
- `PROJECT_MEMORY_ROOT` 改为绝对路径 `/Users/tangwenjing/WeChatProjects/lifeAI`，避免 `~/` 相对拼接错误
- 确认 Memory 根为 `lifeAI/.ai`；误建的 `lifeAI/~/...` 已删除

## 2026-07-12（下午）
- 用户文档写入 `docs/`；刷新 `.ai` 记忆
- 共享页 H5 隐藏扫码；修复聊天发送按钮；超时约 180s；composer 对齐
- 待购买、关键词门禁、饮食历史、体重多条、食物库同名去重等（详见 archive）

更早变更见 `recent-changes.archive.md` 与 `.ai/archive/recent-2026-07.md`。

## 2026-07-12（当前）
- 聊天新增批量记账：识别“记账/收入/支出”文本中的多条金额，先返回确认列表；确认后通过 `/v1/ledger/batch` 事务批量入账
- 聊天新增购物小票/购物明细识别：上传图片并输入“购物明细/小票/账单”等关键词后，AI 提取商品金额，确认后复用批量记账接口入账
- 食物营养表识别时优先使用用户文字输入的食品名称，只有未填写名称时才使用 AI 识别名称
- 修复“上传购物明细图片并输入记账”未触发小票解析的问题，新增“记账+图片”触发条件
- 小票识别增加商品数量字段，支持识别“×2/2件”等，确认入账时数量写入备注，金额保留该行合计金额
- 记账自动触发关键词调整为“支出/收入”，移除“记账”关键词；带图片的小票解析也使用“支出”或购物明细/小票类关键词触发
- 小票金额按总额智能判断单价/行合计：比较原金额合计与“金额×数量”合计，选择更接近小票总额的结果
- 小票解析新增来源选择：其他来源按行金额；京东/淘宝按单价×数量计算
- 图片上传增加用途选择，默认“普通图片”；只有选择“购物小票”后才显示京东/淘宝来源并触发账单解析
- 聊天新增日记更新触发：输入“更新/修改/补充/重写日记”等内容时，立即返回“请稍后到日记模块查看”，后台异步重新汇总并更新昨天的日记
- 记事本新建/编辑改为独立全屏编辑页，移除底部弹窗；通过页面顶部“‹ 返回”返回列表
- 前端 API 地址支持 `VITE_API_BASE_URL`，生产环境可配置 HTTPS 后端；H5 未配置时继续使用同源 `/backend` 代理
- 食物库接口不再返回 `sourceImage`，营养表识别入库也不再保存原图，避免 Base64 图片撑大接口响应
- 历史对话切换增加加载状态与旋转提示，加载期间锁定切换/删除操作，避免重复请求
- 后端请求体默认上限从 20MB 调整为 40MB，支持较大的手机图片上传
- 营养表识别结果增加能量单位兜底：识别到疑似 kJ 的高数值时按 4.184 换算为 kcal 后再入库
- 营养表识别改为后端“识别并入库”一体化接口，避免 AI 响应超时导致后续食物库保存步骤丢失；按用户+名称去重
- 食物营养表识别超过 10 秒时，接口先返回“后台继续处理、稍后到食物库查看”提示，后端异步完成识别和入库
- 新增 `pnpm run build:android` Android App-Plus 构建命令，并补充 HBuilderX 云打包 APK/AAB 说明
- 修复 App-Plus 启动白屏：移除运行时 `import.meta.env`，改用 Vite 构建时注入 API 地址，避免 Android 端 `URL is not defined`
- 首页增加 Android 状态栏与底部安全区动态适配，避免标题顶到状态栏、输入区贴近系统导航栏
- 统一修改 `PageNavbar` 与记事本编辑页，使用系统状态栏高度动态适配 Android 其他模块页面
- 聊天气泡增加 Markdown 表格解析与横向滚动展示，支持 AI 返回的商品/金额等表格内容
- 聊天 Markdown 展示扩展支持标题、粗体、斜体、行内代码、代码块、列表、引用和分隔线
- 聊天文字支持长按选择局部内容，保留整条消息复制按钮；表格单元格和 Markdown 内容同样可选择
- 修复 Android 图片预览路径兼容：使用 App-Plus 私有文档目录生成临时图片，并增加预览失败提示
- 聊天图片改为后端 `uploads/` 文件夹存储，消息数据库只保存 `/v1/uploads/...` 路径；新增图片访问路由，前端自动拼接 API 地址
- 历史会话加载时过滤旧 Base64 图片，只显示新路径型图片；不再兼容旧数据库图片内容
- 美化聊天“复制”按钮：增加圆角胶囊样式、复制图标、按压反馈，并适配用户/AI 气泡颜色
