## 2026-07-12 - Archived Recent Changes

- 新增「待购买」：短期/长期、名称-only、短期可勾选与清空已购；入口在「消费」
- 「功能模块」改名「我的生活」；分组健康/日常/消费/设置
- AI 入库关键词门禁：体重、饮食、记事、日程、购买、收入|支出；食物库需图+「食物」
- 规则优先提取（如体重短句）；ledger / shopping 进入 ExtractionCategory
- 饮食：首页仅今日 +「历史记录」列表/日详情；可 PATCH 重量与日期
- 体重：一天多条，表单日期+时间，列表显示日期时间
- 食物库：识图成功自动入库、同名跳过；手动表单精简为名称/份量/热量

## 2026-07-18 - Archived Recent Changes

- Project Memory MCP：项目级 `.cursor/mcp.json` 已连通（`project-0-lifeAI-project-memory`）
- `PROJECT_MEMORY_ROOT` 改为绝对路径 `/Users/tangwenjing/WeChatProjects/lifeAI`，避免 `~/` 相对拼接错误
- 确认 Memory 根为 `lifeAI/.ai`；误建的 `lifeAI/~/...` 已删除
- 用户文档写入 `docs/`；刷新 `.ai` 记忆
- 共享页 H5 隐藏扫码；修复聊天发送按钮；超时约 180s；composer 对齐
- 待购买、关键词门禁、饮食历史、体重多条、食物库同名去重等（详见 archive）
- 聊天新增批量记账：识别“记账/收入/支出”文本中的多条金额，先返回确认列表；确认后通过 `/v1/ledger/batch` 事务批量入账
- 聊天新增购物小票/购物明细识别：上传图片并输入“购物明细/小票/账单”等关键词后，AI 提取商品金额，确认后复用批量记账接口入账
- 食物营养表识别时优先使用用户文字输入的食品名称，只有未填写名称时才使用 AI 识别名称
- 修复“上传购物明细图片并输入记账”未触发小票解析的问题，新增“记账+图片”触发条件
- 小票识别增加商品数量字段，支持识别“×2/2件”等，确认入账时数量写入备注，金额保留该行合计金额
- 记账自动触发关键词调整为“支出/收入”，移除“记账”关键词；带图片的小票解析也使用“支出”或购物明细/小票类关键词触发
- 小票金额按总额智能判断单价/行合计：比较原金额合计与“金额×数量”合计，选择更接近小票总额的结果
- 小票解析新增来源选择：其他来源按行金额；京东/淘宝按单价×数量计算
- 图片上传增加用途选择，默认“普通图片”；只有选择“购物小票”后才显示京东/淘宝来源并触发账单解析
- 聊天新增日记更新触发：输入“更新/修改/补充/重写日记”等内容时，立即返回“请稍后到日记模块查看”，后台异步重新汇总并更新昨天的日记
- 记事本新建/编辑改为独立全屏编辑页，移除底部弹窗；通过页面顶部“‹ 返回”返回列表
- 前端 API 地址支持 `VITE_API_BASE_URL`，生产环境可配置 HTTPS 后端；H5 未配置时继续使用同源 `/backend` 代理
- 食物库接口不再返回 `sourceImage`，营养表识别入库也不再保存原图，避免 Base64 图片撑大接口响应
- 历史对话切换增加加载状态与旋转提示，加载期间锁定切换/删除操作，避免重复请求
- 后端请求体默认上限从 20MB 调整为 40MB，支持较大的手机图片上传
- 营养表识别结果增加能量单位兜底：识别到疑似 kJ 的高数值时按 4.184 换算为 kcal 后再入库
- 营养表识别改为后端“识别并入库”一体化接口，避免 AI 响应超时导致后续食物库保存步骤丢失；按用户+名称去重
- 食物营养表识别超过 10 秒时，接口先返回“后台继续处理、稍后到食物库查看”提示，后端异步完成识别和入库
- 新增 `pnpm run build:android` Android App-Plus 构建命令，并补充 HBuilderX 云打包 APK/AAB 说明
- 修复 App-Plus 启动白屏：移除运行时 `import.meta.env`，改用 Vite 构建时注入 API 地址，避免 Android 端 `URL is not defined`
- 首页增加 Android 状态栏与底部安全区动态适配，避免标题顶到状态栏、输入区贴近系统导航栏
- 统一修改 `PageNavbar` 与记事本编辑页，使用系统状态栏高度动态适配 Android 其他模块页面
- 聊天气泡增加 Markdown 表格解析与横向滚动展示，支持 AI 返回的商品/金额等表格内容
- 聊天 Markdown 展示扩展支持标题、粗体、斜体、行内代码、代码块、列表、引用和分隔线
- 聊天文字支持长按选择局部内容，保留整条消息复制按钮；表格单元格和 Markdown 内容同样可选择
- 修复 Android 图片预览路径兼容：使用 App-Plus 私有文档目录生成临时图片，并增加预览失败提示
- 聊天图片改为后端 `uploads/` 文件夹存储，消息数据库只保存 `/v1/uploads/...` 路径；新增图片访问路由，前端自动拼接 API 地址
- 聊天图片增加真正的缩略图：后端使用 sharp 保存 `uploads/thumbs/`（列表展示）和 `uploads/originals/`（点击预览）两份文件，数据库保存缩略图路径
- 用户可见品牌名称统一为“小日子AI”，更新首页/H5/App 标题、共享码提示、通知标题和文档标题
- 聊天图片改为应用内全屏预览层，增加“下载图片”按钮；Android 保存到相册，H5 下载文件
- 调整重复日程补建：不再在凌晨无条件扫描；后端每分钟检查一次，仅在当前日程的提醒时间到达后补充下一次记录；App 启动时统一加载未来日程并注册本地提醒，日程页不再承担首次注册职责
- 修复 iPhone 首页上下留白过大：App-Plus 使用系统状态栏/底部安全区变量，避免重复叠加 CSS env 安全区
- 统一修复其他页面的 iPhone 顶部留白：公共 PageNavbar 和记事本编辑导航栏改用单一状态栏高度
- 日程新增/编辑共用表单：选择开始时间后自动填充后一小时结束时间，支持跨午夜；日程列表增加编辑入口
- 修复 iPhone Markdown 列表换行：列表正文统一包裹可伸缩内容节点，避免加粗片段被当作横向项目挤出气泡
- iOS 日程提醒接入 App-Plus Push 模块：App 启动初始化 push，未来日程通过 `plus.push.createMessage` 注册本地通知
- 修复日程保存后立即提示：本地通知改用跨平台 `delay` 秒数，不再使用 iOS 不支持的 `when`；未选择提醒时不注册通知
- 全局品牌更名为“拾光AI”：同步更新首页、应用/H5 配置、通知标题、共享码提示、模型系统提示和后端日志
- 记事本支持文本/列表两种类型：列表按短期待购买清单样式展示，可逐项勾选完成；新增 `Note.type/items` 字段并已应用 MySQL 迁移
- 列表类型记事本改为清单式编辑页：新建时先选择类型，列表页使用“＋添加”、勾选和删除项的短期代购样式，不再使用大段文本框
- 记事本详情编辑页移除类型选择控件，类型仅在点击新建前选择
- 修复 App-Plus 数据共享页二维码生成报错：为 qrcode 动态加载并补充 TextEncoder UTF-8 兼容实现
- 修复 App-Plus 二维码 Canvas 报错：改用静态加载二维码库并生成 SVG data URL，避免代码分割和 Canvas 依赖
- 修复食物库加入饮食默认重量：有包装份量时默认加入一份，无包装份量时默认 100g
- 修正饮食热量换算：有包装每份重量时按“每份营养 ÷ 每份克重 × 实际克重”，无份量信息时才按 100g 作为基准
- Android/iOS App 默认后端地址切换为 `https://113.44.188.18/backend`，H5 仍使用同源 `/backend` 或 `VITE_API_BASE_URL`
- 修复 H5 添加到手机主屏幕无图标：改用 `public/icons` 下的 favicon 与 apple-touch-icon，并补充 iOS 主屏幕元信息
- 后端 build 脚本改为先执行 `prisma generate` 再编译，避免部署服务器 Prisma Client 未同步新字段
- 历史会话加载时过滤旧 Base64 图片，只显示新路径型图片；不再兼容旧数据库图片内容
- 修复食物库加入饮食在清晨日期偏前：改用设备本地日期，不再使用 UTC `toISOString()`；同步修正日程和聊天批量记账默认日期
- 美化聊天“复制”按钮：增加圆角胶囊样式、复制图标、按压反馈，并适配用户/AI 气泡颜色
- 拾光AI-0.0.1.apk：最终签名 APK（包名 uni.app.UNIF35B226，签名 lifeai.keystore）
- lifeai.keystore：签名证书（alias=lifeai, pass=lifeai123, SHA1=AB:2D:A4:01:C3:80:26:E8:69:75:1A:F8:79:97:4C:82:C2:26:A9:78）
